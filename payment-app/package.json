{
  "name": "usdc-payment-app",
  "version": "1.0.0",
  "description": "Simple USDC payment application using EOAs",
  "main": "index.js",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm start",
    "setup": "npm install && cd frontend && npm install"
  },
  "keywords": [
    "usdc",
    "payment",
    "ethereum",
    "eoa",
    "hardhat",
    "react"
  ],
  "author": "USDC Payment Demo",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "ethers": "^6.9.0"
  }
}